spring:
  application:
    name: accm-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/accm
    username: accm
    password: accm
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  threads:
    virtual:
      enabled: true

server:
  port: 8080
